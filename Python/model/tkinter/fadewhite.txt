for i in range(total_steps):
                if current_step <= total_steps:
                    color_val = int((current_step / total_steps) * 255)
                    hex_color = f"#{color_val:02x}{color_val:02x}{color_val:02x}"
                    self.root.config(bg=hex_color)
                    self.ts.bgcolor(hex_color)
                    self.ani.config(bg=hex_color)
                    current_step += 1
                    self.root.after(10)
                    self.root.update()
                else:
                    break
            self.ts.bgcolor("#ffffff")