total_steps = 100
            for current_step in range(total_steps + 1):
                color_val = 255 - int((current_step / total_steps) * 255)
                hex_color = f"#{color_val:02x}{color_val:02x}{color_val:02x}"
                self.root.config(bg=hex_color)
                self.ts.bgcolor(hex_color)
                self.ani.config(bg=hex_color)
                self.root.update()
                self.root.after(20)
            self.ts.bgcolor("black")